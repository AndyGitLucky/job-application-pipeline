# Source Notes

Die technische Betriebs- und Statusdoku wurde nach [PROJECT_STATE.md](./PROJECT_STATE.md) verschoben.

Grund:
- `README.md` im Projektroot ist das Portfolio-/Projekt-README
- `docs/PROJECT_STATE.md` ist die interne technische Doku fuer aktiven Kernpfad, Module, State und Betriebsdetails

Der wichtigste aktuelle Kernpfad ist:

`find_jobs -> score_jobs -> verify_jobs -> Review Workbench`

Persistierte Laufzeitdaten liegen jetzt unter `../runtime/`, generierte Artefakte unter `../artifacts/`.
